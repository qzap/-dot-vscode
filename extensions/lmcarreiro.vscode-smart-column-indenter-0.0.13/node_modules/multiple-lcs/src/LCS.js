"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const RecursiveInGroupsLCS_1 = require("./RecursiveInGroupsLCS");
const IncrementalIntersectionLCS_1 = require("./IncrementalIntersectionLCS");
class LCS {
    //TODO: accept configs of how resolvers to use (default to use all of them)
    constructor() {
        this.lcsResolvers = [];
        this.lcsResolvers.push(new RecursiveInGroupsLCS_1.default());
        this.lcsResolvers.push(new IncrementalIntersectionLCS_1.default());
    }
    execute(sequences) {
        const solutions = this.lcsResolvers
            .map(lcs => lcs.execute(sequences))
            .sort((a, b) => b.length - a.length);
        return solutions[0];
    }
}
exports.default = LCS;
//# sourceMappingURL=LCS.js.map